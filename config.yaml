# Central configuration for bearblog-automation
# Adjust these values when forking this repository

blog:
  # Your blog domain (without https://)
  domain: "fischr.org"
  # Your Bear Blog username (used for export URL)
  bearblog_username: "fischr"

social:
  # Your Mastodon instance URL
  mastodon_instance: "https://mastodon.social"
